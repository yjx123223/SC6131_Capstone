# 项目运行演示 · 录屏脚本

**总时长目标:8–10 分钟** | 每节标注了「操作」(敲什么) 和「旁白」(说什么)

---

## 录制前准备(不录进去)

```bash
cd ~/Desktop/ntu/capstone/Claude_capstone
source .venv/bin/activate
export ANTHROPIC_API_KEY='...'      # 确认已设置
export FRED_API_KEY='...'
pytest tests/ -q                     # 预热并确认环境正常
python main.py --search "apple"      # 预热一次数据加载
clear
```

- 终端字号调大(14pt 以上),窗口调宽,深色背景更清晰
- 提前跑一次 `--multi-agent`,确认 API 可用、耗时心里有数(约 30–60 秒)
- 准备好 `reports/` 里一份已生成的报告备用,万一现场 API 超时可以直接打开讲

---

## 1. 开场(30 秒)

**旁白:**
> 老师好,我演示一下 capstone 项目的运行。这个系统做的事是:把 FinDKG 金融知识图谱里的事件信号,加上实时宏观数据,通过一个多智能体架构生成结构化的投资研究报告。我按数据层、单智能体、多智能体、验证四个部分依次演示。

**操作:** 展示项目目录结构

```bash
ls
```

**旁白:**
> 代码是分层的:`kg_query` 是图谱查询层,`tools/` 是共享工具层,`orchestrator*` 和 `critic` 是智能体层,`tests/` 是测试。

---

## 2. 知识图谱数据层(1.5 分钟)

**操作:**

```bash
head -3 knowledgeGraph/FinDKG/FinDKG_dataset/FinDKG-full/entity2id.txt
cat knowledgeGraph/FinDKG/FinDKG_dataset/FinDKG-full/stat.txt
```

**旁白:**
> 数据集是 FinDKG-full,从华尔街日报新闻抽取的时序知识图谱。13645 个实体、15 种关系、261 个时间步,以周为粒度覆盖 2018 到 2023 年。每条数据是一个四元组:主语、关系、宾语、第几周——本质上是每周新闻事件的结构化快照。

**操作:**

```bash
python main.py --search "tesla"
```

**旁白:**
> 先做个实体模糊搜索,确认图谱里有哪些相关实体。

**操作:**

```bash
python main.py --query-only --entity "Apple Inc."
```

**旁白:**
> 这是 Apple 最近 12 周的原始 KG 事件。注意这里的"最近"是相对数据集末尾 2023 年 1 月,不是今天——数据集是静态历史数据。系统会把这些事件按关系类型归类:`Positive_Impact_On`、`Raise`、`Invests_In` 算正面信号,`Negative_Impact_On`、`Decrease` 算负面,这个极性划分是我们自己定的,数据集本身不带。

---

## 3. 多跳关联查询(1 分钟)

**操作:**

```bash
python -c "
from tools import kg_tools
r = kg_tools.query_kg_signals('Apple Inc.', weeks=12)
print(r['multihop_context'])
"
```

**旁白:**
> 这是最近加的功能:2 跳 BFS 遍历。第一跳是 Apple 的直接关联——控制 App Store、影响 Snap、运营在科技板块;第二跳沿着这些邻居继续走,能挖出间接关联——比如通过"科技板块"这个节点,关联到美联储的影响、中国的影响、以及同板块的 Amazon、Alphabet。这一层是单纯查询做不到的:它揭示的是"风险传导路径",让模型能看到直接事件之外的结构性关联。

---

## 4. 单智能体基线(1.5 分钟)

**操作:**

```bash
python main.py --entity "Apple Inc."
```

**旁白:**
> 这是单智能体模式,作为对照基线:一次性把 KG 摘要拼成 prompt,调一次 Claude,直接返回建议。快、便宜,但没有工具自主选择,也没有质量把关——它拿不到宏观数据,也没人审查它是否过度自信。

*(生成后按回车跳过评分,或演示评一次分)*

---

## 5. 多智能体主演示(3 分钟,核心)

**操作:**

```bash
python main.py --multi-agent --entity "Apple Inc."
```

**旁白(边跑边讲日志):**

> 现在是多智能体模式。注意看日志的执行过程——

> **第 1 轮推理**:模型自主决定调用了三个工具:`query_kg_signals` 查图谱信号、`query_macro` 拉 FRED 实时宏观指标、`get_feedback_stats` 查历史评分统计。工具调用的顺序和参数都不是我写死的,是模型根据任务自己决定的,这是 agentic 的核心。

> **第 2 轮推理**:信息够了,模型调用 `emit_report` 输出结构化草稿。这个工具不是真的去做什么事,而是用 JSON schema 强制模型按固定字段输出——执行摘要、宏观分析、个股分析、配置建议、置信度、关键信号,一个都不能少。

> **Critic 审查**:草稿完成后,一个独立的 Critic Agent 用干净的上下文重新审查,检查三件事:KG 信号和宏观信号方向是否冲突、置信度和数据量是否匹配、结论是否有数据支撑。它输出结构化 JSON 审查意见。如果不通过,会触发一轮修订——修订时用 `tool_choice` 强制再次调用 `emit_report`,保证修订稿依然符合 schema。

> **报告保存**:最终渲染成 Markdown 存到 `reports/`。

---

## 6. 报告解读(1 分钟)

**旁白(滚动展示报告):**

> 报告结构:执行摘要给结论,宏观和个股分析分开论证,配置建议是五档枚举之一,关键信号列出支撑依据,最后是风险提示。

> 特别指出两点。第一,**工具调用轨迹**——报告末尾附了完整的 trajectory,每一步调了什么工具、什么参数都可追溯,这是可审计性的保证,不是黑箱输出。

> 第二,注意这里:模型引用了"Invests_In 类信号历史准确率 0.786"——这个数字来自我们的反馈闭环数据库,是真实的历史评分统计,说明模型确实把历史反馈用进了推理。

---

## 7. 反馈闭环(1 分钟)

**操作:**

```bash
python main.py --report
```

**旁白:**
> 这是信号准确率报告。每次建议生成时,系统会把当时的 KG 信号快照存进 SQLite;用户事后评分后,就能按关系类型统计哪类信号历史表现更好。可以看到 `Invests_In` 的平均评分明显高于 `Decrease`。这些统计通过 `get_feedback_stats` 工具回灌给智能体,让它知道哪类信号更可信——这就形成了闭环。

**操作(可选):**

```bash
python main.py --history
```

---

## 8. 回测验证(1 分钟)

**操作:**

```bash
python main.py --rolling "Apple Inc."
```

**旁白:**
> 最后是信号验证。因为项目没有股价数据,我设计了一个用 KG 自身验证的方案:站在历史时间点 T,只用 T 之前 12 周的数据判断信号方向;然后看 T 之后 8 周实际出现的事件是否与判断同向,算一个 [-1,1] 的一致性得分;滚动做 10 个窗口,得出平均分和胜率。

> 需要说明的是:**这不是收益回测,验证的是信号的自洽性和持续性**,新闻信号到股价之间还隔着市场定价这一层。但它的价值在于严格避免了 look-ahead bias——判断只用 T 之前数据,验证只用 T 之后数据。

---

## 9. 工程质量(30 秒)

**操作:**

```bash
pytest tests/ -q
```

**旁白:**
> 52 个单元测试,1 秒跑完,全部不依赖真实 API 和真实数据集:用合成的最小图谱测查询层,用 stub 的 LLM client 按脚本回放响应来测 agentic loop 的各种终止路径。这样测试零成本、确定性,可以进 CI。

---

## 10. 收尾:已知局限与下一步(30 秒)

**旁白:**

> 最后说两个已知问题。第一是**时间错位**:KG 数据截止 2023 年 1 月,但宏观数据是实时的,报告会把 2022 年底的公司事件和当前宏观环境当作同一截面分析,而且 Critic 目前也查不出来——因为传给它的上下文摘要里丢掉了日期字段。修复方向是在 prompt 里明确标注数据时段,并把时效性检查加进 Critic 的审查清单。

> 第二是 Critic 在 JSON 解析失败时默认放行,兜底策略偏宽松,应该改成标记为不可信。

> 下一步的重点是系统性评估:用滚动回测量化信号胜率,以及做单智能体和多智能体的报告质量对照,这部分会作为论文的实验章节。

---

## 应急预案

| 情况 | 处理 |
|---|---|
| API 超时/报错 | 打开 `reports/` 里已生成的报告继续讲,说明"这是刚才跑的结果" |
| 数据加载慢 | 提前预热过就不会;真慢就顺势讲数据规模 |
| Critic 恰好审查通过没触发修订 | 正常情况,说明"这次草稿质量达标,不通过时会触发修订,我可以展示代码里的修订逻辑" |
| 老师追问回测有效性 | 直接承认是信号一致性而非收益回测,讲清 look-ahead bias 的规避 |

---

## 一页速记(录制时贴在屏幕边上)

```
1. 开场            30s   分层结构
2. 数据层         1.5m   stat.txt → --search → --query-only
3. 多跳            1m    multihop_context
4. 单Agent        1.5m   --entity(基线对照)
5. 多Agent         3m    --multi-agent(4工具/2轮/Critic/修订)
6. 报告解读        1m    trajectory + 0.786
7. 反馈闭环        1m    --report
8. 回测            1m    --rolling(非收益回测!)
9. 测试           30s    pytest 52 passed
10. 局限+下一步    30s   时间错位 + 评估计划
```
